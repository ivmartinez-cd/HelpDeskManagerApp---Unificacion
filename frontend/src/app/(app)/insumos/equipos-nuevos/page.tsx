/** SCAFFOLD — Equipos Nuevos (`/insumos/equipos-nuevos`).
 *
 * Placeholder de la fundación: reemplazar por la pantalla real
 * (`/api/insumos/new-devices` + sync + marcar ignorados). */
export default function InsumosEquiposNuevosPage() {
  return <div className="px-9 py-8">Equipos Nuevos</div>;
}
