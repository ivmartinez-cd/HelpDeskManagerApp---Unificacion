/** SCAFFOLD — Estadísticas globales de Insumos (`/insumos/estadisticas`).
 *
 * Placeholder de la fundación: reemplazar por la pantalla real
 * (`/api/insumos/estadisticas` + Patrón 2 + rankings). */
export default function InsumosEstadisticasPage() {
  return <div className="px-9 py-8">Estadísticas de Insumos</div>;
}
