/** SCAFFOLD — Dashboard de Insumos (`/insumos`).
 *
 * Placeholder de la fundación: reemplazar el contenido por la pantalla real
 * (tabla de clientes Patrón 1 + tiles KPI + acciones + modales). El módulo NO
 * tiene hub de herramientas: `/insumos` es directamente el Dashboard. */
export default function InsumosDashboardPage() {
  return <div className="px-9 py-8">Dashboard de Insumos</div>;
}
